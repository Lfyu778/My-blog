# 从零读懂这份博客：技术栈与学习路径

> **读者背景**：只会基础 HTML / CSS  
> **目标**：搞清楚这个博客「用了什么、每部分干什么、该按什么顺序学」  
> **原则**：所有例子都来自本项目真实代码，不写教科书空例子

---

## 一、先建立全局图景：这个博客到底是什么？

### 1.1 一句话概括

这不是手写一堆 `.html` 页面的传统网站，而是：

**用 Markdown 写文章 → 用 VitePress 自动生成网站 → 部署到 Vercel 上线**

你写的是 `.md` 文件，浏览器看到的是完整的 HTML 页面。中间那层「把 Markdown 变成网站」的工具，就是 **VitePress**。

### 1.2 和你熟悉的 HTML 对比

| 传统手写网站 | 这份博客 |
|---|---|
| 每个页面写一个 `.html` | 每篇文章写一个 `.md` |
| 导航、侧边栏每个页面复制一遍 | 在配置文件里写一次，所有页面自动带上 |
| 自己管样式、布局、响应式 | 主题已经做好，你只改颜色和细节 |
| 改完要自己上传服务器 | `git push` 后 Vercel 自动部署 |

### 1.3 项目结构（先认目录，不急着读代码）

```
my-blog/
├── package.json              ← 项目说明书：依赖什么、怎么启动
├── package-lock.json         ← 依赖精确版本锁定（先忽略）
├── vercel.json               ← 告诉 Vercel 怎么构建和部署
├── .gitignore                ← 告诉 Git 哪些文件不要提交
│
└── docs/                     ← ⭐ 真正的内容都在这里
    ├── index.md              ← 首页
    ├── about.md              ← 关于页
    ├── guestbook.md          ← 留言板页
    ├── posts/                ← 文章目录
    │   ├── agent/            ← Agent 分类
    │   └── cs/               ← CS 分类
    └── .vitepress/           ← VitePress 的「引擎室」
        ├── config.mts        ← 站点配置（导航、侧边栏、标题…）
        ├── public/           ← 静态资源（favicon 等）
        └── theme/            ← 主题定制
            ├── index.ts      ← 主题入口
            ├── custom.css    ← 自定义样式
            └── components/
                └── GiscusComment.vue  ← 留言板组件
```

**记忆口诀**：
- 写文章 → 改 `docs/` 下的 `.md`
- 改导航/侧边栏 → 改 `config.mts`
- 改颜色/样式 → 改 `custom.css`
- 加交互功能 → 写/改 `.vue` 组件

---

## 二、技术栈全景图（按学习顺序）

按「必须先会 → 才能理解后面」排列。标了 ⭐ 的是核心，其余是增强。

```
第 0 层（你已会）
  └── HTML + CSS 基础

第 1 层（立刻能上手）⭐
  ├── Markdown          ← 写文章的语言
  ├── Node.js + npm     ← 装工具、跑命令
  └── 终端基础命令      ← cd / npm / git 的最小集

第 2 层（理解「网站是怎么生成的」）⭐
  ├── VitePress 概念    ← 静态站点生成器
  ├── frontmatter       ← Markdown 文件头部的配置
  └── VitePress 配置    ← config.mts 里那些选项

第 3 层（理解「定制和扩展」）
  ├── CSS 变量 / 主题   ← custom.css 里的 :root / .dark
  ├── 一点点 JavaScript ← 动态创建 DOM、挂载脚本
  ├── Vue 3 组件入门    ← GiscusComment.vue
  └── TypeScript 入门   ← 类型标注长什么样（能读即可）

第 4 层（上线与协作）⭐
  ├── Git + GitHub      ← 版本管理 + 远程仓库
  ├── Vercel 部署       ← 自动构建上线
  └── Giscus 留言系统   ← 用 GitHub Discussions 做评论
```

下面按层展开：**是什么 → 在本项目哪里用到 → 你要学到什么程度 → 推荐从哪读**。

---

## 三、第 1 层：立刻能上手

### 3.1 Markdown —— 写文章的语言

**是什么**  
一种用纯文本写格式的轻量标记语言。`#` 是标题，`**粗体**`，`-` 是列表。VitePress 会把它转成 HTML。

**在本项目哪里**  
所有文章和页面：`docs/posts/**/*.md`、`about.md`、`guestbook.md` 等。

**你要学到什么程度**  
会写标题、段落、列表、链接、代码块、引用就够了。进阶再学表格和 VitePress 扩展语法（如 `::: tip`）。

**项目中的例子**（`docs/guestbook.md`）：

```markdown
---
title: 留言板
---

# 💬 留言板

欢迎在这里留下你的想法、建议、或者任何问题。

::: tip 如何留言
点击下方评论区，使用 GitHub 账号登录即可留言。
:::

<GiscusComment />
```

注意三件事：
1. 文件最上面 `---` 包起来的叫 **frontmatter**（见 4.2）
2. `::: tip` 是 VitePress 扩展，不是标准 Markdown
3. `<GiscusComment />` 是 Vue 组件，可以写在 Markdown 里（见 5.3）

**推荐练习**：打开任意一篇 `docs/posts/` 下的文章，对照渲染结果看每个符号对应什么效果。

---

### 3.2 Node.js + npm —— 项目的「运行时」和「包管理器」

**是什么**
- **Node.js**：让 JavaScript 能在电脑上跑（不只在浏览器里）
- **npm**：Node 的包管理器，负责安装别人写好的工具（比如 VitePress）

**在本项目哪里**  
`package.json` 就是项目的「说明书」：

```json
{
  "name": "my-blog",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  }
}
```

读法：
| 字段 | 含义 |
|---|---|
| `scripts.dev` | 本地开发：启动预览服务器 |
| `scripts.build` | 构建：把 Markdown 编译成静态 HTML |
| `scripts.preview` | 本地预览构建结果 |
| `devDependencies` | 开发时需要的依赖（VitePress、Vue） |

**你要会的命令（背下来）**：

```bash
npm install      # 根据 package.json 安装依赖 → 生成 node_modules/
npm run dev      # 启动开发服务器，浏览器打开 localhost:5173
npm run build    # 生成最终网站到 docs/.vitepress/dist/
```

**你要学到什么程度**  
不需要会写 Node 程序。只要理解：`package.json` 声明依赖 → `npm install` 下载 → `npm run xxx` 执行脚本。

---

### 3.3 终端最小命令集

在项目里你几乎只会用到这些：

```bash
cd 路径              # 进入目录
npm install          # 装依赖
npm run dev          # 本地预览
git status           # 看改了什么
git add .            # 暂存改动
git commit -m "说明" # 提交
git push             # 推到 GitHub（触发自动部署）
```

Windows 上用 Git Bash / PowerShell / Windows Terminal 都可以。

---

## 四、第 2 层：网站是怎么生成的

### 4.1 VitePress 是什么？

**VitePress** = 基于 Vite + Vue 的**静态站点生成器（SSG）**。

工作流程：

```
你写的 Markdown          VitePress 构建          浏览器看到的
docs/**/*.md      →     编译 + 套主题     →    一堆 .html/.css/.js
config.mts               注入导航侧边栏          完整可访问网站
theme/*                  应用自定义样式
```

「静态」的意思：最终产物是普通 HTML 文件，不需要服务器跑数据库或后端逻辑，所以可以免费托管在 Vercel / GitHub Pages 上。

**为什么选它**（结合本项目）：
- 默认主题已经有导航、侧边栏、搜索、暗色模式、目录
- 用 Markdown 写，学习成本低
- 需要交互时可以嵌 Vue 组件（留言板就是这样）

---

### 4.2 Frontmatter —— 给页面加「元数据」

Markdown 文件顶部用 `---` 包起来的 YAML 区域，叫 frontmatter。VitePress 用它控制页面行为。

**首页**（`docs/index.md`）用了特殊布局：

```markdown
---
layout: home

hero:
  name: "我的技术博客"
  text: "记录学习、思考与成长"
  tagline: "一个计算机专业学生的技术探索空间……"
  actions:
    - theme: brand
      text: 开始阅读
      link: /posts/agent/what-is-agent
    - theme: alt
      text: 关于我
      link: /about

features:
  - icon: 🤖
    title: Agent 开发
    details: AI Agent 的设计思路……
    link: /posts/agent/what-is-agent
---
```

- `layout: home` → 使用首页布局（大标题 + 按钮 + 功能卡片）
- `hero` / `features` → 首页内容的结构化数据，不是手写 HTML

**普通页面**只需要标题：

```markdown
---
title: 关于
---
```

**你要学到什么程度**  
会改 `hero`、`features`、`title`；知道 frontmatter 是「页面配置」，正文是「页面内容」。

---

### 4.3 站点配置 `config.mts` —— 博客的控制台

这是整个站点最重要的配置文件：`docs/.vitepress/config.mts`

```ts
import { defineConfig } from 'vitepress'

export default defineConfig({
  title: '我的技术博客',
  description: 'AI Agent 开发、CS 基础、项目实战……',
  lang: 'zh-CN',

  themeConfig: {
    nav: [ /* 顶部导航 */ ],
    sidebar: { /* 侧边栏，按路径分组 */ },
    socialLinks: [ /* GitHub 图标 */ ],
    footer: { /* 页脚文字 */ },
    search: { provider: 'local' },  // 本地搜索
    outline: { label: '本页目录', level: [2, 3] },
    docFooter: { prev: '上一篇', next: '下一篇' },
  },

  markdown: {
    lineNumbers: true  // 代码块显示行号
  }
})
```

**导航栏 `nav`**（顶部菜单）：

```ts
nav: [
  { text: '首页', link: '/' },
  {
    text: '文章',
    items: [  // 下拉菜单
      { text: 'Agent 开发', link: '/posts/agent/what-is-agent' },
      { text: 'CS 基础', link: '/posts/cs/tcp-handshake' },
    ]
  },
  { text: '留言板', link: '/guestbook' },
  { text: '关于', link: '/about' },
]
```

**侧边栏 `sidebar`**（文章页左侧，按 URL 前缀匹配）：

```ts
sidebar: {
  '/posts/agent/': [
    {
      text: '🤖 Agent 开发',
      items: [
        { text: '什么是 AI Agent？…', link: '/posts/agent/what-is-agent' },
        { text: '提示词工程：…', link: '/posts/agent/prompt-engineering' },
      ]
    }
  ],
  '/posts/cs/': [ /* 同理 */ ]
}
```

**关键认知**：
- 新建一篇文章后，**不会自动出现在侧边栏**，要手动在 `sidebar` 里加一条
- 链接路径对应 `docs/` 下的文件路径（不含 `.md`）
  - 文件：`docs/posts/agent/what-is-agent.md`
  - 链接：`/posts/agent/what-is-agent`

**文件扩展名 `.mts`**：TypeScript 模块。你暂时把它当「带类型提示的 JS 配置文件」即可，语法几乎和 JS 一样。

---

## 五、第 3 层：定制与扩展

### 5.1 CSS 变量与主题 —— 你最容易上手的部分

你已经会 CSS，这里只是多了两个概念：

**① CSS 自定义属性（变量）**

`docs/.vitepress/theme/custom.css`：

```css
:root {
  --vp-c-brand-1: #4f46e5;   /* 主品牌色 */
  --vp-c-brand-2: #6366f1;
  --vp-c-bg: #fafafa;         /* 背景色 */
  --vp-c-text-1: #1c1917;     /* 主文字色 */
}

.dark {
  --vp-c-brand-1: #818cf8;    /* 暗色模式下换一套值 */
  --vp-c-bg: #0f0f0f;
}
```

VitePress 主题大量使用 `--vp-c-*` 变量。你改变量，整个站点颜色跟着变，不用一个个选择器去改。

**② 覆盖默认组件样式**

```css
/* 首页大标题做成渐变字 */
.VPHero .name .clip {
  background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 50%, #ec4899 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

/* 功能卡片悬停上浮 */
.VPFeature:hover {
  transform: translateY(-4px);
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.08);
}
```

这些 class（`.VPHero`、`.VPFeature`、`.VPNavBar`…）是 VitePress 默认主题自带的，你在浏览器开发者工具里审查元素就能看到。

**页面内嵌样式**  
Markdown 里也可以写 `<style>`，只影响当前页。见 `docs/index.md` 的「最近文章」卡片、`docs/about.md` 的关于卡片。

**你要学到什么程度**  
- 会改 `:root` / `.dark` 里的颜色变量  
- 会用开发者工具找到 class，写覆盖样式  
- 理解「CSS 变量 + 主题 class」= 低成本换肤

---

### 5.2 一点点 JavaScript —— 只为读懂留言板

你不需要系统学 JS，但要能看懂下面这段在干什么。

`GiscusComment.vue` 的核心逻辑：

```ts
const giscusRef = ref<HTMLDivElement>()

onMounted(() => {
  const script = document.createElement('script')  // 创建一个 <script> 标签
  script.src = 'https://giscus.app/client.js'      // 指向 Giscus 官方脚本
  script.setAttribute('data-repo', 'lfyu778/my-blog')
  script.setAttribute('data-repo-id', 'REPO_ID_HERE')
  // ... 更多 data-* 配置
  script.async = true
  giscusRef.value?.appendChild(script)             // 插入到页面里
})
```

翻译成你熟悉的思路：

```
页面加载完成后：
  1. 动态创建一个 <script> 标签
  2. 设置它的 src 和各种 data- 属性（配置项）
  3. 把它塞进页面的某个 div 里
  → 浏览器加载这个脚本 → Giscus 评论框出现
```

对应的 HTML 部分：

```html
<div ref="giscusRef" class="giscus-container"></div>
```

`ref="giscusRef"` 让 JS 能拿到这个 div，相当于「给这个元素起了个可在脚本里用的名字」。

**你要学到什么程度**  
能读懂「创建元素 → 设属性 → 插入 DOM」三步，不要求会独立写复杂逻辑。

---

### 5.3 Vue 3 组件入门 —— 留言板组件拆解

**Vue** 是前端框架，用来把「结构 + 逻辑 + 样式」打包成可复用的组件。VitePress 底层就是 Vue。

一个 `.vue` 文件通常三块（本项目完整示例）：

```vue
<script setup lang="ts">
// 逻辑：数据、生命周期
import { onMounted, ref } from 'vue'
const giscusRef = ref<HTMLDivElement>()
onMounted(() => { /* 插入 Giscus 脚本 */ })
</script>

<template>
  <!-- 结构：类似 HTML -->
  <div ref="giscusRef" class="giscus-container"></div>
</template>

<style scoped>
/* 样式：scoped 表示只作用于本组件 */
.giscus-container {
  margin-top: 32px;
  min-height: 200px;
}
</style>
```

**主题入口如何注册组件**（`docs/.vitepress/theme/index.ts`）：

```ts
import DefaultTheme from 'vitepress/theme'
import type { Theme } from 'vitepress'
import GiscusComment from './components/GiscusComment.vue'
import './custom.css'

export default {
  extends: DefaultTheme,           // 继承默认主题（导航、侧边栏都还在）
  enhanceApp({ app }) {
    // 注册为全局组件 → 任意 Markdown 里都能写 <GiscusComment />
    app.component('GiscusComment', GiscusComment)
  },
} satisfies Theme
```

所以 `guestbook.md` 里那一行 `<GiscusComment />` 才能生效。

**你要学到什么程度**
- 知道 `.vue` = template + script + style
- 知道 `extends: DefaultTheme` 是「在默认主题上增强」
- 知道 `app.component(...)` 是「注册全局组件」
- 暂时不用会写复杂 Vue 应用

---

### 5.4 TypeScript —— 能读就行

项目中出现的 TS 特征：

```ts
import type { Theme } from 'vitepress'   // 只导入类型
const giscusRef = ref<HTMLDivElement>() // <HTMLDivElement> 是类型参数
} satisfies Theme                        // 约束对象符合 Theme 形状
```

**你要学到什么程度**  
把 `<...>`、`type`、`satisfies` 当成「给编辑器看的注释」，不影响运行逻辑。以后真要写配置再系统学。

---

## 六、第 4 层：上线与协作

### 6.1 Git + GitHub

**Git**：本地版本管理。每次 `commit` 是一个快照。  
**GitHub**：远程仓库托管 + Discussions（留言板依赖它）。

本项目日常循环：

```
改 Markdown / 配置
    → git add .
    → git commit -m "add: 某篇文章"
    → git push
    → Vercel 自动重新部署
```

`.gitignore` 告诉 Git 忽略什么：

```
node_modules/           # 依赖太大，不该进仓库（别人 npm install 即可）
docs/.vitepress/dist/   # 构建产物，线上再生成
docs/.vitepress/cache/  # 缓存
```

---

### 6.2 Vercel 部署

`vercel.json`：

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "docs/.vitepress/dist",
  "framework": "vitepress"
}
```

含义：
1. 收到代码推送后，执行 `npm run build`
2. 把 `docs/.vitepress/dist` 当作网站根目录发布
3. 用 VitePress 相关的默认优化

你不需要自己管服务器、HTTPS、CDN——这些 Vercel 全包了。

---

### 6.3 Giscus 留言系统

**原理**：评论存在你 GitHub 仓库的 Discussions 里，页面通过嵌入 Giscus 脚本显示。

依赖条件：
1. 仓库开启 Discussions
2. 安装 Giscus GitHub App
3. 在 `GiscusComment.vue` 填入真实的 `data-repo-id` 和 `data-category-id`（目前还是占位符）

数据流：

```
访客在网页留言
  → Giscus 脚本
  → 写入 GitHub Discussions
  → 其他人刷新页面就能看到
```

完全免费，不用自己的数据库。

---

## 七、数据流总览（串起来）

```
┌─────────────────────────────────────────────────────────┐
│  你（作者）                                              │
│  写 docs/**/*.md  +  改 config.mts / custom.css / .vue  │
└──────────────────────────┬──────────────────────────────┘
                           │ git push
                           ▼
┌─────────────────────────────────────────────────────────┐
│  GitHub 仓库                                             │
│  存源码；Discussions 存留言                              │
└──────────────────────────┬──────────────────────────────┘
                           │ 触发 Webhook
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Vercel                                                  │
│  npm install → npm run build → 发布 dist/                │
└──────────────────────────┬──────────────────────────────┘
                           │ HTTPS
                           ▼
┌─────────────────────────────────────────────────────────┐
│  访客浏览器                                              │
│  看文章（静态 HTML）+ 留言（Giscus 动态加载）            │
└─────────────────────────────────────────────────────────┘
```

本地开发时没有 GitHub/Vercel，路径更短：

```
改文件 → npm run dev → 浏览器 localhost:5173 热更新预览
```

---

## 八、推荐学习顺序（可执行计划）

按周估算，每天 1–2 小时。可按实际速度压缩或拉长。

### 第 1 周：跑起来 + 会写文章

| 天 | 做什么 | 对应文件/命令 |
|---|---|---|
| Day 1 | 安装 Node.js，`npm install` + `npm run dev`，浏览每个页面 | 终端、浏览器 |
| Day 2 | 学 Markdown 基础语法，对照现有文章看效果 | `docs/posts/**/*.md` |
| Day 3 | 改首页文案、关于页自我介绍 | `docs/index.md`、`docs/about.md` |
| Day 4 | 新建一篇测试文章，写入 frontmatter | `docs/posts/` 下新建 `.md` |
| Day 5 | 在 `config.mts` 的 nav / sidebar 加上这篇新文章 | `docs/.vitepress/config.mts` |
| Day 6–7 | 改品牌色、字体，用开发者工具找 class 做小样式调整 | `custom.css` |

**本周验收**：能独立完成「写文章 → 挂到导航/侧边栏 → 改点颜色 → 本地预览」。

---

### 第 2 周：理解配置与主题

| 天 | 做什么 | 对应文件 |
|---|---|---|
| Day 1 | 逐项读 `config.mts`，改 title / footer / outline，观察变化 | `config.mts` |
| Day 2 | 弄清 sidebar 路径匹配规则；试着加一个新分类文件夹 | `docs/posts/新分类/` |
| Day 3 | 精读 `custom.css` 的 `:root` 和 `.dark`，做一套自己的配色 | `custom.css` |
| Day 4 | 读懂 `index.md` 的 `layout: home` + hero + features | `docs/index.md` |
| Day 5 | 读 `theme/index.ts`，理解「继承默认主题 + 注册组件」 | `theme/index.ts` |
| Day 6–7 | 对照 HTML 知识，把 `GiscusComment.vue` 的三块结构标注释 | `GiscusComment.vue` |

**本周验收**：能向别人讲清楚「改导航改哪、改颜色改哪、留言板组件怎么挂上的」。

---

### 第 3 周：Git 上线 + 留言板

| 天 | 做什么 |
|---|---|
| Day 1–2 | 学 Git 最小集：status / add / commit / push / pull |
| Day 3 | 推到 GitHub（若还没有远程仓库） |
| Day 4 | 接 Vercel，拿到线上域名，验证自动部署 |
| Day 5–6 | 按项目已有教程配置 Giscus（开 Discussions、填 repo-id） |
| Day 7 | 走通完整闭环：本地写文章 → push → 线上可见 → 留言成功 |

**配套阅读**：仓库根目录的 `VitePress博客从零到上线教程.md`（操作步骤很详细，本指南补「为什么」）。

**本周验收**：朋友能打开你的链接看文章并留言。

---

### 第 4 周及以后：按需加深

只在「你想改某个功能却改不动」时再学，避免空学：

| 想做的事 | 需要补的知识 |
|---|---|
| 更复杂的页面交互 | Vue 3 基础（响应式、组件通信） |
| 自动根据文件生成侧边栏 | 一点 Node.js 脚本 |
| 文章列表不手写 HTML | VitePress 的数据加载 / 或主题扩展 |
| 更好的 SEO、sitemap | VitePress 相关插件 |
| 类型报错能自己修 | TypeScript 入门 |
| 自己的域名 | DNS / CNAME 基础（教程里有） |

---

## 九、代码阅读顺序（带着问题读）

不要从上到下扫文件，按问题驱动：

```
① 网站内容从哪来？
   → docs/index.md
   → docs/about.md
   → docs/posts/agent/what-is-agent.md

② 导航和侧边栏谁控制的？
   → docs/.vitepress/config.mts

③ 颜色、圆角、悬停效果谁控制的？
   → docs/.vitepress/theme/custom.css
   → 各 md 文件底部的 <style>

④ 留言板怎么嵌进去的？
   → docs/guestbook.md          （使用 <GiscusComment />）
   → theme/index.ts             （注册组件）
   → theme/components/GiscusComment.vue  （实现）

⑤ 项目怎么启动、怎么上线？
   → package.json               （scripts + 依赖）
   → vercel.json                （构建与输出目录）
   → .gitignore                 （哪些不进仓库）
```

---

## 十、概念对照速查（HTML/CSS 视角）

| 你已熟悉的 | 本项目对应概念 | 说明 |
|---|---|---|
| `.html` 文件 | `.md` 文件 | 内容载体；构建时变成 HTML |
| `<title>` / meta | frontmatter + `config.mts` 的 title/description | 页面和站点元信息 |
| 手写导航 `<nav>` | `themeConfig.nav` | 配置化，一处修改全局生效 |
| 手写侧边栏 | `themeConfig.sidebar` | 按路径前缀匹配 |
| `style.css` | `custom.css` + 页面内 `<style>` | 用 CSS 变量覆盖主题 |
| CSS 颜色写死 | `--vp-c-brand-1` 等变量 | 一处改，全局换肤；还支持暗色 |
| 复制粘贴的页头页脚 | 默认主题布局 | 你几乎不用写公共布局 |
| 无 | Vue 组件 `.vue` | 把交互封装成标签，Markdown 里直接用 |
| 无 | `package.json` / npm | 管理工具链依赖 |
| 手动 FTP 上传 | `git push` + Vercel | 自动构建部署 |
| 无后端评论 | Giscus + GitHub Discussions | 无服务器的评论方案 |

---

## 十一、学习时的常见坑

1. **改了文章侧边栏没出现**  
   必须在 `config.mts` 的 `sidebar` 里手动加链接。

2. **链接 404**  
   路径是 `/posts/agent/what-is-agent`（对 `docs/posts/agent/what-is-agent.md`），不要写 `.md`，也不要写错文件夹名。

3. **`npm run dev` 改了没反应**  
   多数情况会热更新；若改的是 `config.mts`，有时需要重启 dev 服务器。

4. **把 `node_modules` 拷给别人**  
   不要。让对方自己 `npm install`。这也是 `.gitignore` 忽略它的原因。

5. **留言板空白**  
   检查：仓库是否 Public、Discussions 是否开启、`REPO_ID_HERE` 是否已替换、Giscus App 是否安装到该仓库。

6. **只学工具不写内容**  
   技术是为表达服务的。第 1 周就可以开始写真正的笔记，边写边学配置。

---

## 十二、你现在就可以做的 3 件事

1. **今天**：`npm run dev`，一边点页面一边在编辑器里打开对应 `.md`，建立「文件 ↔ 页面」映射。  
2. **本周**：改 `about.md` 成你的真实介绍，改 `custom.css` 的品牌色成你喜欢的颜色。  
3. **本月**：写 1 篇真文章 + 走通 GitHub + Vercel 上线。

---

## 附录：技术名词极简表

| 名词 | 一句话 |
|---|---|
| 静态站点生成器（SSG） | 构建时把内容变成 HTML 文件的工具 |
| Vite | 前端构建工具，VitePress 基于它，开发时很快 |
| Vue | 前端 UI 框架，组件化开发 |
| TypeScript (TS) | 带类型的 JavaScript，减少低级错误 |
| Markdown | 用符号表示格式的轻量文本 |
| Frontmatter | Markdown 文件头的 YAML 配置区 |
| npm | Node.js 的包管理器 |
| Node.js | 在电脑上运行 JavaScript 的环境 |
| Git | 代码版本管理工具 |
| GitHub | Git 仓库托管 + 协作平台 |
| Vercel | 自动部署前端/静态站点的云平台 |
| Giscus | 基于 GitHub Discussions 的评论组件 |
| CDN | 把静态文件分发到全球节点，访问更快 |
| 热更新（HMR） | 改代码后浏览器几乎瞬间刷新，不用手动 F5 |

---

*这份指南对应该仓库当前源码结构。若你之后大改目录或换主题，以实际文件为准，学习路径仍然适用。*
